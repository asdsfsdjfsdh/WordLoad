---
title: 生存 Run 肉鸽体验优化方案
version: 1.0
created: 2026-08-14
status: 待评审
tags: [设计分析, 单词之旅, 生存Run, 肉鸽]
aliases: [肉鸽优化方案]
---

# 生存 Run 肉鸽体验优化方案 v1

> 配套 [[生存模式执行计划.md]]（v2.4，数值与里程碑基线，实际代码已到 v2.7）。本文档是对**已上线**的肉鸽体验做的诊断 + 优化提案，逐条标注对应代码位置，不做无依据的空想。
>
> **核心结论先行**：读完全部相关代码后发现，系统本身的深度并不差——`survival.ts` 里已经有基于关键词标签的隐藏 buff 协同（雷+击→超载、霜+御→冰甲、愈+击→吸血），`FlashCard.tsx` 里已经有"看词跟写→听音默写→回忆拼写"三段提示衰退 + 拼错回队尾的间隔巩固。**但这些深度相当一部分对玩家不可见、不可达**：协同系统没有任何 UI 提示，材料合成接口 (`POST /materials/synthesize`) 做了后端但前端完全没有入口，战斗中选完 buff 后再也看不到自己身上有什么。你反馈的"缺乏惊喜感 / 不够丰富"，很大一部分根因是**深度被埋起来了**，而不是深度不存在。
>
> 因此本文档的优化提案分两类：① **挖出已有深度**（成本低，见效快，优先做）；② **补真正缺失的内容**（成本中高，需要设计取舍）。

---

## 一、现状盘点：已经做对的部分

先列清楚，避免后面的提案变成推倒重来。

| 维度 | 已具备的能力 | 代码位置 |
|---|---|---|
| 数值驱动 | 双端数值 θ 公式、怪 HP/速度随天数增长、对攻曲线，全部纯函数 + 蒙特卡洛仿真标定 | `packages/shared/src/game.ts`、`apps/api/src/runs/balance-sim.ts` |
| Buff 分层 | 普通(白/蓝/紫，稀有度按天解锁) / 传说(首领后单局一次) / 永久(局外强化)三层 | `game.ts` `BUFF_DEFS`/`NORMAL_BUFF_POOL`/`LEGEND_BUFF_POOL` |
| **隐藏协同** | buff 带 `tags`（御/击/愈/霜/雷/火），`resolveEffects` 里有真实的关键词合成：雷≥1+击≥2→超载(连击门槛-1)、霜≥1+御≥1→冰甲(受击冻结全场)、愈≥1+击≥2→吸血 | `survival.ts:80-127` |
| 上下文感知 buff 池 | 按 HP 低/正确率高低/首领前动态调整候选优先级 | `apps/api/src/runs/buff-picker.ts` |
| 双粒度 SRS | 词级 `UserWordProgress` 管掌握度、义项级 `UserSenseProgress` 管下次考哪个义项 | `db/schema.prisma` |
| 记忆提示衰退 | 预习页对"不认识"词做 看词跟写 → 听音默写 → 回忆拼写 三段，拼错的词重排到复核队尾（间隔重试） | `apps/web/src/components/FlashCard.tsx:474-586` |
| 复习优先级 | 错词 > 已学未二测 > 日历到期 > 将到期 > 低掌握度，五级优先级 | `apps/api/src/runs/review-queue.ts` |
| 新词注入门控 | 轻量 Q（错词数）驱动 + 保底 5 + 严格隔天 + acc 门控/强制停注入，防止"复习空转"或"弱者被新词淹没" | `apps/api/src/runs/inject.ts` |
| Boss 双驱动 | 累计新词量 + 天数保底双触发，"学一批新词就检验一次"的隐喻 | `apps/api/src/runs/boss-trigger.ts` |
| 战斗表现 | 连击 ×3/×5/×7 里程碑特效分级（发光/震波粒子/全屏波纹+减速）、Boss 击破庆祝、程序生成音效覆盖大部分事件 | `apps/web/src/components/BattleField.tsx` |
| 结算双反馈 | 生存结果页同时展示"生存收益"(天数/纪录/Boss次数)与"学习收益"(新认识/复习/掌握/易错) | `apps/web/src/components/RunFlow.tsx:720-802` |

---

## 二、四个痛点的根因诊断

### 痛点 1：buff / 养成缺乏惊喜感

| 现象 | 代码证据 | 归因 |
|---|---|---|
| 同样处境下反复打，三选一总是那几个 | `pickBuffs()` **零随机源**：`hp/maxHp/acc/day` 一旦相同，`pushIfAvailable` 按固定顺序数组推入，结果 100% 确定 (`buff-picker.ts:42-88`) | 游戏设计：上下文感知本身没错，但"感知"和"决定论"被做成了同一件事——理想的肉鸽应该是"感知收窄候选池，权重加成，但仍在池内随机" |
| 有协同机制却从没"哇"过 | `resolveEffects` 里的雷/击/愈/霜/御 关键词合成是纯后台数值，`tags` 字段全项目只在这一处被读取（已用 grep 确认），前端从没渲染过 | UI + 游戏设计交叉问题：深度存在但零反馈，玩家永远不知道自己在"拼配方" |
| 选完 buff 就像没选一样 | `BattlePage.tsx`/`RunFlow.tsx` 战斗阶段（`phase==='wave'`）全文搜索 `buffs`/`buffMeta` **无匹配**——选完 buff 之后的整个波战期间没有任何常驻 UI 告诉玩家"你现在有什么" | UI 问题：没有心理锚点，构筑感无从谈起 |
| 养成显得单薄 | `CharacterPage.tsx` 只展示 tier-1 材料数量(`mat1Count`)；`POST /materials/synthesize`（3×tierN→1×tier(N+1)）在 `apps/api/src/materials/` 已实现，但前端 `apps/web/src` 全局搜索 `synthesize` **零匹配** | 游戏设计 + 工程债：一层完整的"低稀有→高稀有"元进程做了后端，从没上线给玩家 |

### 痛点 2：记忆效果不确定

| 现象 | 代码证据 | 归因 |
|---|---|---|
| 不知道哪个字母拼错的 | `TypingCore.tsx:100-172` 的 `commit()` 只在整词填满后判定一次 `correct`，`errored` 只是把已填的槽位整体标红，不做逐字母 diff | 记忆学：缺少"错误定位"这一步，学习者无法精确修正记忆表征，只能整词重来 |
| 首字母提示对所有词一视同仁 | `question-builder.ts:203-207`：中译英模式恒定"首字母给出+其余全挖空"，听写模式恒定"首尾给出"，**与该词的掌握度(`mastery`)完全无关** | 记忆学：违反"合意难度"(desirable difficulty)——生词和已答对 20 次的词得到同样强度的提示，无法通过渐退提示制造递增的检索难度 |
| 战斗内"巩固重写"是抄写，不是回忆 | `TypingCore.tsx:344-388` 膨胀重写模式：先亮出正确答案+例句，再让玩家抄 3 遍；对照 `FlashCard.tsx` 的预习页是"看→听→忆"三级提示衰退 | 记忆学 + 一致性问题：同一款应用里，"预习阶段"给了更科学的检索强度台阶，"实战答错阶段"反而退化成纯抄写，两条路径强度不统一 |
| 易混词完全从战斗消失 | v2.2 决策把 `note`(形近/音近提示)整个移出战斗、只留在图鉴 (`项目计划-v1.md` 方案修订 v2.2 第二节) | 记忆学：为了防剧透把"对比学习"这个最有效的辨析手段连答错后的补救环节都一并砍掉了，属于"防剧透"和"促进辨析"两个目标没有做折中，而是选择了其中一端 |
| 没有词根/联想编码 | 全仓库搜索 `词根`/`etymology`/`morphem`/`助记`/`mnemonic` **零匹配**；`Word` 模型只有 `phoneticAm/phoneticEn/difficultyScore/tier/difficultyDims`，没有词源/词缀/记忆锚点字段 (`db/schema.prisma:24-44`) | 记忆学：目前唯一的编码通路是"生成效应"(自己拼出来)，没有第二层"精细加工"(elaboration)编码，长难词/超纲词全靠死记硬背 |
| 隐藏状态对玩家不可见 | `recentAcc`(近 20 题正确率) 驱动 buff 池、注入量、强制停注入三件大事，但 `apps/web/src` 全局搜索找不到任何展示"近期正确率"的 UI | 记忆学：自我调节学习(self-regulated learning)需要对自己表现的实时反馈，现在这条反馈回路完全在后端打转 |

### 痛点 3：战斗 / UI 反馈不够爽

| 现象 | 代码证据 | 归因 |
|---|---|---|
| 平时（连击 <3）手感偏平 | `comboFx()` 只在 combo===3/5/7 触发（`BattleField.tsx:353-390`），普通命中走 `attack()`/`explode()` 基础粒子，没有随连击数递增的"渐强"反馈 | 游戏设计：里程碑式反馈让 80% 的操作（combo 1-2）显得平淡，理想是每次命中都有反馈，只是强度分级 |
| 感受不到"节奏在变化" | 音频层只有事件音效 (`sfx.ts` 18 个离散音效函数)，没有随天数/危险度变化的背景层 | UI/氛围：数值上"怪越来越硬、天越来越难"这条核心张力线，声音和视觉节奏上没有对应表现 |
| 看不到自己的构筑 | 同痛点 1 的"无常驻 buff HUD" | 与痛点 1 共因，见上 |

### 痛点 4："不够丰富" + "拼写不够深刻"（用户原话）

这两条其实是前三个痛点的汇总症状：

- "不够丰富" = 内容广度不足（普通 buff 只有 12 种、传说只有 6 种、永久成长只有 3 个数值维度）**加上**已有内容不可见（协同、材料合成、rolling acc）—— 双重稀薄化。
- "拼写不够深刻" = 记忆编码只有一层（生成效应），没有精细加工层（词根/联想/对比辨析），且提示强度不随掌握度变化，导致"背了很多遍但印象不深"的主观感受。

---

## 三、优化提案

### P0：挖出已有深度（成本低，建议优先做）

1. **常驻 buff HUD**：战斗界面加一个角落面板，图标化展示当前已选 buff（含叠加层数），复用 `BUFF_DEFS.icon`/`rarityClass`。这是后面协同可视化、以及"惊喜感"的地基。
2. **协同可视化**：buff 卡片上直接印关键词标签（御/击/愈/霜/雷/火），命中 `resolveEffects` 里的组合阈值时（如雷≥1+击≥2）在 HUD 或选择页弹一次"⚡ 超载已激活"提示。不需要改动引擎逻辑，只是把已经算出来的 `CombatEffects` 结果渲染出来。
3. **材料合成 UI**：`CharacterPage.tsx` 补一个"合成"卡片，展示 tier1~4 全部材料持有量 + 调用现成的 `POST /materials/synthesize`。后端已完工，纯前端工作量。
4. **"近期状态"可见化**：把 `recentAcc` 之类驱动 buff/注入决策的隐藏状态，做成战斗 HUD 里的一条低调状态条（例如一个"节奏"小图标 + 简短文案"手感火热/平稳/需要复习"），不用暴露具体数字和公式，但要让玩家能建立"我打得怎样 → 系统给我什么"的因果直觉。

### P1：游戏设计向

5. **buff-picker 引入受控随机**：保留"上下文收窄候选池"的设计，但在满足条件的候选内加权随机（而不是数组固定顺序推入），同状态下重开一局仍应有不同的三选一。同时可以引入"重抽"资源型消耗（如金币重抽 1 次/天），让玩家对不满意的选项有主动权。
6. **扩充 buff/传说池 + 明确协同"配方"**：当前 tags 组合只有 3 个隐藏配方，建议扩到 5~8 个，并在图鉴或养成页做一个"配方图鉴"（类似杀戮尖塔的卡牌/遗物百科），哪怕不追求赛前查阅，赛后结算页揭示"你本局触发了：超载、冰甲"也能制造"原来还能这样"的复盘惊喜。
7. **永久成长维度扩展**：目前只有 hp/atk/def 三维度强化，建议引入 1~2 个"特化"方向消耗高阶材料（tier3/4），如"斩杀词根"（对长难词首击伤害额外加成）、"复习专精"（复习词吸血翻倍），让高阶材料有比"多囤一点三维强化"更有玩法意义的出口。

### P1：记忆学向

8. **拼写错误的逐字母反馈**：`commit()` 判定时逐字符 diff，把错误的具体位置标红/闪烁，而不是整词染色。改动集中在 `TypingCore.tsx` 的 `renderSlots` 与判定逻辑，无需动服务端契约。
9. **提示强度与掌握度绑定**：`question-builder.ts` 的挖空策略接入 `mastery`/`reviewStage` 参数——新词/低掌握度给首字母提示，高掌握度逐步收紧到"仅音标/仅释义，无字母提示"，制造随复习次数递增的合意难度。这是一个纯函数改动 + 一批新单测，改动面可控。
10. **统一"答错巩固"与"预习"的提示衰退强度**：把 `TypingCore.tsx` 里战斗内膨胀重写的"先给答案抄 3 遍"，改造成与 `FlashCard.tsx` 一致的"看词跟写 → 听音默写 → 回忆拼写"三级，两条路径共用同一套衰退曲线（可以提取为 `packages/shared` 或一个共享 hook）。
11. **补一层最小可行的精细加工编码**：不需要一次性上线 AI 词根管线（v1.1 范围），可以先给 `Word`/`WordSense` 加一个可选的"记忆锚点"字段（一句话联想/词根提示），由词库贡献者/AI 离线批量生成后人工抽查，战斗内答错巩固阶段展示。即使覆盖率不到 100% 也能验证方向。
12. **易混词对比的折中方案**：不在正常答题时剧透，但在"答错进入巩固"这一步（本来就已经在展示正确答案+例句了）顺带展示"注意区分：principal ≠ principle"。这是在 v2.2 已有的膨胀重写 UI 里加一行文案，成本很低，且不违反"平时不剧透"的原设计初衷。

### P2：UI / 反馈向

13. **基础连击也要有分级反馈**：把 combo 1/2 也给一个更轻量的反馈层级（比如轻微的描边高光 + 短促音效变调），让"里程碑特效"变成在"持续有感"基础上的加强，而不是从无到有的突变。
14. **动态音乐/氛围层**：在离散音效之外叠加一层随危险度（HP 比例、临近首领波）渐变的氛围音轨或滤波效果，用听觉持续传达"张力在升级"，配合已有的对攻曲线设计意图。

---

## 四、建议实施顺序

| 阶段 | 内容 | 备注 |
|---|---|---|
| Step 1 | P0 全部四项 | 都是"已有后端/已有数据，缺前端展示"，工作量小、验证快，建议一次性做完看效果 |
| Step 2 | 8 / 9 / 10 / 12（记忆学向的核心四条） | 集中在 `TypingCore.tsx` + `question-builder.ts`，改动面相对集中，且直接回应"拼写不够深刻"这条最具体的反馈 |
| Step 3 | 5 / 6（buff 随机化 + 协同配方图鉴） | 需要重新过一遍 M3 的纯函数单测和平衡仿真（改随机源要重新盯一下方差，不能破坏 `balance-sim.ts` 的中位存活 5-15 天目标带） |
| Step 4 | 7 / 11 / 13 / 14 | 内容量和美术/音频工作量更大，建议放在验证完前三步的方向之后再投入 |

---

## 五、需要你确认或做用户验证的开放问题

- "不够丰富"具体是指哪个环节最先让你有这个感觉——是选 buff 的时候、养成页、还是拼单词本身？这决定 Step 1 里四项的相对优先级。
- 战斗内是否希望保留"不剧透"的克制（即提案 12 的折中方案是否符合你的设计红线），还是你其实更倾向直接把易混提示放回战斗内？
- 词根/联想锚点（提案 11）如果要做，数据从哪来——纯人工标注、还是接一次性 AI 批量生成 + 人工抽查（对应 v1 计划里本来就留了口子的 v1.1 AI 标注管线）？这决定这条能不能在近期启动。
