# 单词之旅 · 生存 Run 执行计划 (v2.4)

> 版本 2.4 · 创建 2026-08-13 · 配套 `项目计划-v1.md`（本计划为 v2 生存模式的独立执行依据，实施进度以本文件为准）
> 一句话：把战斗改为**无限生存 Run**——答对吸血、状态跨天继承、断点可续、千词池内持续注入新词、新词首击暴击、Boss 双驱动检验、分层肉鸽 buff + 永久养成，形成"背词=生存能力"的完整闭环。
> v2.3 变更：按 §4.9 蒙特卡洛仿真标定数值（maxHp 8→20、吸血 8:1→6:1、答错 0.15/day→0.05/day、Boss base 10→5、首领战前 +4→+6），验证中位存活 5–15 天目标带。数据源见 `survival-final.mjs`。
> v2.4 变更（四角度评审结论）：①养成补**合成系统**（3×tierN→tier(N+1)），高稀有材料有出口；②注入门控改**轻量 Q + 保底注入 5**，合格玩家每天有新词；③对抗接入**词难度 tierFactor**（越难的词怪越硬）+ **速度/眩晕驱动漏怪模型**（连错 2 眩晕暂停逼近）；④M3 仿真升级并重标定 §4.9。

## 1. 目标与设计决策（已确认）

| # | 决策 |
|---|---|
| 1 | 战斗模式 → **生存模式**：一局 Run 连续多"天"，目标=活得更久（最高生存天数=该阶段纪录） |
| 2 | **断点续 Run**：Run 状态服务端持久化，中途退出可续 |
| 3 | **状态继承**：HP 跨天继承；恢复靠**答对吸血**（每答对 N 题回 1 HP） |
| 4 | **肉鸽 buff**：每活过一天三选一（局内叠加有上限）+ **永久养成**（金币+材料升级三围） |
| 5 | **千词池持续注入**：Run 绑定当前 stage 词池（每阶段 1000+ 词，不跨阶段承接）；新词从池中持续注入（每天 ≤15），**怪物属性随 day 无限上升** → 玩家必然逐渐败北，"纪录天花板"由对攻曲线交汇点决定（决策 A） |
| 6 | **新词首击×2**：新词（本局首次出场）答对时攻击伤害×2，出场一次后本局回归普通伤害（奖励新词记忆）（决策 B） |
| 7 | 复习按**记忆风险优先级**排序，新词与旧词**交错出题** |
| 8 | 新词首战日：**先入战前预习页**再出战 |
| 9 | **Boss 双驱动出现**：每累计消耗 20 新词触发一次检验 + 天数保底（首见 day3 / 最短隔 2 天 / 最长隔 4 天）；Boss=学习里程碑，击破额外奖励；首领波前给一次本局小回复/必推「免伤」buff，防死亡螺旋（决策 F） |
| 10 | 主动收枪：金币×0.5、不计破纪录；死亡结算全额（决策 C） |
| 11 | 全局仅一个 active Run，开新 Run 自动结算旧的（决策 D） |
| 12 | 扣血公式纳入玩家属性，随时间上升形成对攻曲线（决策 E） |
| 13 | 注册时**自动创建 3/3/3 角色**（不再手动 init） |
| 14 | **buff 分层**：普通 buff（每天三选一）+ 传说技能（首领战后单局一次，改机制）+ 永久强化（局外） |
| 15 | **上下文感知选择池**：HP<30% 必含防御、acc 高加权进攻、剔除已满 buff、首领前必含对策项 |
| 16 | **首领奖励节奏**：战前 +6 HP + 必含免伤；战后稀有掉落 + 传说选择（奖励总在挑战后） |

## 2. 数据模型（db/schema.prisma + 迁移）

```prisma
model Run {
  id              Int      @id @default(autoincrement())
  userId          Int
  bankId          Int
  stageId         Int                // 绑定当前 stage，不跨承接
  mode            String   @default("zh2en")
  day             Int      @default(1)          // 当前天数
  hp              Int                           // 服务端权威血量
  maxHp           Int                           // 含本局buff加成
  buffs           Json     @default("[]")       // 已选局内buff
  lastInjectDay   Int      @default(0)          // 注入冷却（严格隔天交替）
  surrendered     Boolean  @default(false)      // 主动收枪
  recordBroken    Boolean  @default(false)      // 破纪录幂等双保险
  extra           Json     @default("{}")       // 连续低准确天数等
  status          String   @default("active")   // active / finished
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  items      RunItem[]
  user       User      @relation(...)
  bank       WordBank  @relation(...)

  @@index([userId, status])       // /runs/active 查询
  @@index([userId, stageId])      // 破纪录 max 查询
}
model RunItem {
  id        Int     @id @default(autoincrement())
  runId     Int
  seq       Int
  wordId    String
  senseIdx  Int     @default(0)
  type      String  // new / review / boss / retest(错题重测)
  answered  Boolean @default(false)
  correct   Boolean?
  elapsedMs Int     @default(0)
  run       Run     @relation(fields: [runId], references: [id], onDelete: Cascade)
  word      Word    @relation(fields: [wordId], references: [id])

  @@index([runId])
  @@unique([runId, seq])
}
```

要点：
- Run 同时充当历史纪录：finished 行按 (user,stageId) 取 `day` 最大值 = 最高生存天数；**破纪录奖励**仅当结算 day 严格大于历史最大值且 `recordBroken` 置位（防刷+幂等双保险）。
- 逐天题目持久化为 RunItem（answered=false 的即"当前波次未答题"，是续 Run 的还原点）。
- **本局新词 = 词池中该 Run 内 RunItem 首现的词**；`type='new'` 仅首现那一条，之后同词重测标 `retest/review`。

## 3. API 契约

| 接口 | 请求 → 响应 | 说明 |
|---|---|---|
| POST `/runs` | `{ bankCode, stageId, mode? }` → `{ run, day:1, hp, maxHp, questions, previewWords, injectedNew }` | 已有 active Run → 自动结算旧的（收枪语义）再开新 Run；首日播 20 词（新词优先） |
| GET `/runs/active` | → `{ run, questions(未答), previewWords, injectedNew, ended?:false }` 或 `null` | 断点续 Run；无 active 返回 null |
| POST `/runs/:id/advance` | `{ answers, buffChoice? }` → `{ day, hp, maxHp, buffs, bossWave, bossCleared, ended, result?, questions, previewWords, injectedNew, nextDayNewWords }` | 波末提交：算吸血/扣血→注入判断→(首领波)→下一波题；HP≤0 或词尽→自动 finish |
| POST `/runs/:id/finish` | `{ answers?, surrender: boolean }` → `RunFinish` | 主动收枪/死亡结算：rating+天数+首领数→XP/金币/材料/破纪录；收枪 → coins×0.5、不计破纪录 |
| POST `/auth/strengthen` | `{ stat: 'hp'\|'atk'\|'def' }` → `AuthUser` | 金币+精华消耗→三围+1 |
| POST `/materials/synthesize` | `{ fromTier: 1\|2\|3 }` → `{ materials }` | 合成：3×tierN → 1×tier(N+1)，扣手续费金币 |

- `RunFinish` 在现有 `SessionFinish` 基础上扩展：`bestDays, recordBroken, bossClearedCount, daysSurvived`。
- **防作弊**：`advance/finish` 的 `answers[]` 每项**必带 `typed`**，服务端比对为准（复用现有 `resolveCorrect`），缺失返回 400；吸血/扣血/奖励全以服务端判定为准。
- **防并发**：`advance/finish` 复用现有乐观锁模式（`status='active'` 守卫 + `updateMany count` 校验 + 事务），防并发双注入/双奖励。
- 中途关页：无需 keepalive 终局提交——答案未答项保留在 DB，续 Run 重答；已答项只在 advance 时落库（波形中断最多丢当波，可接受，实时持久化排 v1.1）。

## 4. 核心规则（全部进 shared 配置，可调）

### 4.1 新词注入（千词池自适应门控）
```
Q_light = 本局错词数（w>0 的词，含首领战失误）              // 轻量 Q：只计"没掌握的"
acc     = 最近 20 题滚动正确率
L       = 本 stage 词池中本局未出场词数                  // 千词池，正常 Run 不枯竭
inject  = clamp(max(5, 15 − Q_light), 0, 15)，且 acc<0.75 → 0；acc 连续两日<0.65 → 强制停注入
约束    : 注入日 = 严格隔天交替（lastInjectDay 记录）；首领波日不注入；L=0 → inject=0（后台兜底）
```
- **保底注入 5**（v2.4）：acc≥0.75 的合格玩家每天至少注入 5 个新词，保证 Boss 双驱动（每 20 新词）与新词首击×2 机制长期激活；Q_light 只计错词，不再把"已答对 1 次"的词计入阻塞注入，避免"复习空转"。
- 效果：高手积攒清空快→稳定有新词；弱者（acc<0.75）→几乎纯复习；注入后次日新词首战，先入预习页。
- **千词池保证注入长期非零**：正常 Run（死于对攻曲线 10–20 天）远到不了池尽，注入永不枯竭。
- 词尽兜底 finish（后台判定，几乎不触发）：池内全部 mastery=100 且无到期复习；**死亡为主要终局**，纪录天花板 = 怪物对攻曲线交汇点。

### 4.2 复习优先级（记忆风险倒序）
1. 本局错词（首领战失误优先，后随错次衰减）
2. 本局学过未二次复测词
3. 日历到期词（`nextReviewAt≤now`，最过期先）
4. 将到期词（今+1 天）
5. 池内 mastery<50

新词与复习**交错**排波（interleaving）。

### 4.3 双端数值 θ = 天 × 词难度 × 成长
| 项 | 公式 |
|---|---|
| tierK | Ⅰ:1.0 Ⅱ:1.3 Ⅲ:1.6 Ⅳ:2.0（阶段档位）|
| tierFactor | `{Ⅰ:1.0, Ⅱ:1.25, Ⅲ:1.6, Ⅳ:2.0}`（**词级难度**，v2.4：恢复"统一难度源"，越难的词怪越硬）|
| dayK | `1 + 0.12·(day−1)`，封顶 2.8 |
| 怪 HP | `max(2, ceil(baseHp·tierFactor(word)·dayK / atkMult / (1+dmgBuff)))`（baseHp=2；v2.4：怪属性取**当前词难度**，与仿真/战斗共用同一公式）|
| 怪速度 | `baseSpeed·sqrt(tierK)·(1+0.02·day)`，封顶 +30% |
| 逼近预算 | `travelBudget = clamp(ceil(速度基准 / speedMult(day)), 4, 12)`（题数内未击杀 → 漏怪；速度基准 12，v2.4 重标定）|
| atkMult | `1 + 0.25·(atkLv−1)`（+局内 buff，仍受下限约束）|
| defRed | `min(0.4, 0.1·(defLv−1))`，受击 `≥1` |
| maxHp | `20 + 2·hpLv`（+局内 buff，起手 26/36/42 随等级）|
| 吸血 | 每答对 6 题回 1 HP（`吸血+1` buff 使 N−2，最低 3）|
| 保底 | 怪所需答对数 **≥2**（设计红线：碾压压不到 1 击）→ 基础 1HP 怪（Spike/Shard）基值上调至 2 |
| 新词首击 | 本局首现词答对时 `dmg × 2`，出场后本局回归普通伤害 |
| 首领波 | 触发规则见 §4.8（双驱动，非 day%3）；`bossHp=ceil(5·tierK·(day/3)/atkMult)`（基值 5，见平衡标定）；P2 于血过半进入（失误 -1~4、P2 段题需答对 2 次）；击破+稀有掉落；**开打前+6 HP 小回复或必含「免伤」选项** |

**玩家伤害模型（决策 E，属性上升对攻曲线）**：
| 事件 | 原始伤害 raw（随 day 上升） | 实际扣血 |
|---|---|---|
| 答错 | `1 + 0.04·(day−1)`，封顶 2 | `max(1, ceil(raw·(1−defRed)))` |
| 漏怪（怪抵达） | `1 + 0.1·(day−1)`，封顶 3 | 同上 |
| Boss 失误 | `1 + 0.2·(day−1)`，封顶 4 | 同上 |
| 眩晕（连错 2） | 0（怪暂停禁答） | — |

**漏怪/眩晕时间驱动模型（v2.4，M3 仿真与战斗共用）**：
```
逐问循环：答对 → 攻击"最近怪"，命中后扣除怪 HP（新词首击×2 记 2 击）
          答错 → 记 1 错；连错 2 → 眩晕：该回合怪不逼近、展示答案、输入禁答
漏怪判定：场上怪随时间逼近（每问 tick 推进）；**仅"最近怪"（前锋）预算耗尽未击杀 → 抵达漏怪**（后排怪未到身侧不提前漏），漏后补位新怪
场上怪数：≤5 自动补位（沿用 v1），新怪 HP 按当前词 tierFactor 计算；**每天总怪数 ≈ ceil(20/3)≈7，逐问错峰入场**（避免同时铺满 5 只造成提前漏怪）
```
- 对攻曲线：`maxHp=20+2·hpLv` 与吸血 6:1 对抗"伤害随 day 增长"，交汇点 = 个人纪录天花板；def/hpLv 上移交汇点 → 养成有实感。
- 波内 HP 客户端预测显示，**死亡判定只在波末 advance**（服务端权威）。
- 新词首击×2 **不适用于 Boss 段**（Boss 段统一普通伤害）。

### 4.4 肉鸽 buff 池（分层）
| 层 | 频率 | 内容 | 特点 |
|---|---|---|---|
| 普通 buff | 每天末三选一 | `+2 本局maxHp(≤3次)` / `伤害+1(≥2击保底)` / `吸血+1(≤2次)` / `冻结加时` / `免伤1次(≤2次)` | 数值微调：容错与效率 |
| 传说技能 | 首领战后三选一（**单局一次**） | `P2 免伤免疫` / `击杀回血` / `Boss 段伤害×2` / `漏怪不扣血` | 改变机制，构筑记忆点 |
| 永久强化 | 局外（CharacterPage） | 三围+1（金币+材料，成本递增，上限≤等级+4） | 元进程，吃材料稀有度 |

只给容错与效率，不给答案。

**上下文感知选择池（决策 15）**：
- `HP < 30% maxHp` → 必含 1 项防御（+maxHp / 免伤）
- 近 20 题 `acc < 0.75` → 加权 吸血+1 / 免伤（弱势保命）
- `acc ≥ 0.85` → 加权 伤害+1 / 冻结加时（高手提速）
- 已达叠加上限的 buff → 从池剔除
- 首领前一日 → 必含 1 项"首领对策"（免伤 / Boss 伤害）

> 原则：选择是**困境的回应**，不是随机的奖励——系统根据生存状态推荐，决策从"抽卡"变成"策略"。

### 4.5 奖励与纪录
- 结算：`xp = ratingExp + 3·min(day, 20)`（天数加成封顶，防后期通胀）；`coins = 答对数×2 + (SSS?10) + 首领数×5 + day`；**主动收枪 ×0.5**
- 材料稀有度按到达天数抬升：day≥3 解锁 Ⅱ，≥5 解锁 Ⅲ，≥8 解锁 Ⅳ（8 天封顶）；**Ⅲ/Ⅳ 供合成链与破纪录 bonus 消耗**（见 §4.6）
- 破纪录（首次活过第 N 天）：一次性 bonus 金币+高稀有精华，仅当 `day > 历史最大` 且 `recordBroken` 置位（幂等，仅死亡结算触发；收枪不计）
- SRS 词级+义项级更新复用现有结算核心（重构为可复用函数）

### 4.6 strengthen 消耗表（进 shared 配置）
| stat | 金币 | 材料 |
|---|---|---|
| hp | 60 | 2× 生命精华(tier1) |
| atk | 40 | 2× 力量碎片(tier1) |
| def | 50 | 2× 守护结晶(tier1) |

三围上限 ≤ 角色等级 + 4（沿用 v1"等级解锁强化上限"）。

**合成系统（材料出口，防高稀有溢出）**：
```
3× tierN 材料  +  手续费 20·N 金币  →  1× tier(N+1) 材料      (N=1,2,3)
```
- 目的：Ⅲ/Ⅳ 材料（day≥5/8 抬升产出）除破纪录 bonus 外有稳定消耗通道，避免长线溢出；也给弱玩家"攒低稀有→合成高稀有"的养成路径。
- 幂等防并发：复用 strengthen 的乐观锁 + 事务模式；材料数量校验失败返回 409。
- 合成不触发破纪录、不计天数奖励，纯材料通道。

### 4.7 波次锚点
`QUESTIONS_PER_DAY = 20`（天↔波↔题数唯一锚点；注入日题量 = 复习量 + 注入量，浮动）。

### 4.8 Boss 出现规则（双驱动，决策 9）
```
BOSS_WORD_INTERVAL = 20    // 每累计消耗 20 个新词 → 触发一次首领波
minGapDays        = 2      // 两次首领波至少隔 2 天（防连续压力过载）
firstBossDay      = 3      // 保底第 3 天出现首次首领波（新手缓冲）
maxGapDays        = 4      // 距上次首领 4 天未触发 → 强制首领波（防拖）

触发判定（波末 advance）：
  if 距上次首领 < minGapDays      → 不触发
  elif 本局累计新词消耗 ≥ 上次首领时 + BOSS_WORD_INTERVAL → 触发
  elif 距上次首领 ≥ maxGapDays    → 强制触发
  elif day == firstBossDay 且尚未打过首领 → 触发
```

> 核心隐喻：**"掌握一批新词，就面临一次检验"**——Boss 从"计时器事件"变成"学习里程碑"。高手学得快→检验勤（压力与进度正相关）；拖沓玩家最迟每 4 天被强制检验（防"不注入就不会死"的消极玩法）。

首领波日不注入；首领波在当天题段末尾压轴（波内而非会话段）。

### 4.9 平衡标定（M3 蒙特卡洛仿真结果，已达标 · v2.4）
> 目标带：中位存活 **5–15 天**。以下参数为命中目标带的标定值（v2.4 模型：词 tier 加权 HP + 速度/眩晕漏怪 + 轻量 Q 保底注入 5），全部进 shared 配置。数据源 `apps/api/src/runs/balance-sim.ts`（`CALIBRATED`，`pnpm --filter @word-journey/api exec ts-node src/runs/balance-sim.ts` 复现表 1–4）。

| 参数 | 标定值 | 依据 |
|---|---|---|
| maxHp | `20 + 2·hpLv` | 起手 26，给 75% 正确率玩家约 6 天中位 |
| 吸血 N | 6 | 日净流出接近 0 时维持长线 |
| 答错 raw | `1 + 0.04·(day−1)` cap 2 | 0.75 正确率 → 日均 5 错，配合漏怪可承受 |
| 漏怪 raw | `1 + 0.1·(day−1)` cap 3 | 前锋漏怪（时间驱动）量级低于答错，久漏为患 |
| Boss 失误 | `1 + 0.2·(day−1)` cap 4 | 首领战非秒杀，失误可容 |
| BOSS_BASE | 5 | day3 首 Boss 连对≈4 击，75% 正确率可过 |
| 首领战前回复 | +6 HP | 防死亡螺旋 |
| tierFactor | `{1.0, 1.25, 1.6, 2.0}`（Ⅰ/Ⅱ/Ⅲ/Ⅳ） | 词难度接入对攻：越难的词怪越硬 |
| 怪 HP 基值 | baseHp=2 | 保底 ≥2 击设计红线 |
| 每天怪数 | ceil(20/3)=7 | 错峰入场 ≤5 场，漏怪压力可控 |
| 逼近速度基准 | 12（travelBudget 4–12） | 慢逼近 + 连错眩晕 → 弱玩家有喘息 |
| 漏怪/答错封顶 | 漏 3 / 错 2 | 后期以 Boss 与 dayK 为主压力源 |

**仿真结论**（acc∈{0.6,0.7,0.75,0.85,0.92}×3000 局, 三围 3/3/3, v2.4 模型）：
- 中位存活 4 / 4 / **6** / 9 / 16 天；≥15 天占比 0 / 0 / 0 / 3.5 / 67.5%
- 随机三围分布（acc=0.75）：1/1/1→中位3，3/3/3→6，5/5/5→8，8/8/8→11
- 敏感性验证：0.75 正确率中位 6 天命中目标带，0.85 中位 9 天、0.92 中位 16 天，符合"弱者纯复习、高手长线"意图；Boss 均破 0.50/0.88/1.07/1.97/3.96
- 归因：v2.4 引入词 tier 加权后怪更硬、时间驱动漏怪更陡，初版参数中位仅 2–3 天，死亡主因=场上 5 怪同时逼近提前漏怪；修正为**仅前锋漏怪 + 每天 7 怪错峰入场 + 逼近速度基准 12** 后，曲线平滑进入目标带

## 5. 落地顺序（里程碑）

| M | 内容 | 关键文件 |
|---|---|---|
| M1 | 数据与类型 | `schema.prisma`(+Run/RunItem 含索引与幂等字段)+迁移；`shared/api.ts`(+Run/RunFinish/strengthen)；`shared/game.ts`(+数值表/buff/注入阈值/伤害模型/QUESTIONS_PER_DAY/消耗表) |
| M2 | 后端核心 | `apps/api/src/runs/*`（新 controller/module/service）；`boss-trigger.ts`（双驱动判定）；`settlement.ts` 重构为 items 驱动可复用；`auth`+strengthen；DTO |
| M3 | 后端纯函数+测试 | `inject.ts`(注入)、`review-queue.ts`(优先级)、`theta.ts`(θ公式/伤害模型/新词首击)、`boss-trigger.ts`(双驱动)、`buff-picker.ts`(上下文感知)、破纪录/奖励纯函数 + Jest；**平衡仿真**（0.6/0.75/0.9 正确率 ×3000 局蒙特卡洛 → 存活天数分布，目标中位 5–15 天，先于 M4 验证死亡天花板）。**已完成**：`balance-sim.ts`+`balance-sim.spec.ts`（v2.4 模型，§4.9 重标定值已验证，10 断言） |
| M4 | 前端战斗 | `BattleField.tsx`：θ 公式、吸血回HP与"+♥"飘字、怪顶"需答对X题"、首领波、新词首击暴击表现、传说技能状态钩子（击杀回血/漏怪不扣血等） |
| M5 | 前端 Run 流程 | `BattlePage.tsx` 状态机：预习页(新词首战日分支，复用 FlashCard/列表+斩词)→波战→天转换→普通buff三选一(上下文感知)→首领战→战后传说选择→继续/收枪(衰减结算)；续 Run 恢复 |
| M6 | 外壳收尾 | `CharacterPage` 强化按钮；`StageMapPage` 显示该 stage 最高生存天数；注册自动建 3/3/3 角色；`LobbyPage` 微调 |
| M7 | 调参验证 | 一局感亲手验：首局手感、dayK 曲线、吸血 N（标定 6）、注入阈值、首领压力（BOSS_WORD_INTERVAL/minGap/maxGap）、伤害斜率、传说技能强度；以 §4.9 标定值为基线 |

## 6. 测试
- 纯函数：injection（acc/Q/L 边界、注入冷却）、review 优先级、θ 公式、**伤害模型边界（defRed 封顶、受击≥1）**、**新词首击×2（首现 vs 二次）**、**boss-trigger（minGap/maxGap/firstBoss/学习量累计、不注入不触发）**、**buff-picker 上下文感知（HP/acc 分支、满叠剔除、首领前必含对策）**、**传说技能单局一次约束**、rating/天数奖励、破纪录幂等
- **平衡仿真**：0.6/0.75/0.9 正确率曲线 ×3000 局，输出存活天数分布，断言中位数落在目标带（5–15 天）；v2.4 增补：**保底注入 ≥5 且 ≤15**、**眩晕日确有输出（连错 2 触发）**、**tierⅣ 怪需击数 ≥ tierⅠ**、**逼近预算封顶 [minTravel, maxTravel]**；以 §4.9 标定值为回归基线
- 接口：建 Run→advance→续 Run 恢复→finish；**advance 强制 typed（缺失 400）**；hp 服务端权威（防客户端改血）；**并发 advance/开新 Run 不双结算**；strengthen 余额扣减与上限；防刷（重启不重复破纪录）
- 现状复用：`wordIds` 强制新词、SRS 到期逻辑、Boss 连对战、`pagehide` 保底

## 7. 风险与护栏
| 风险 | 应对 |
|---|---|
| 单场马拉松 | 断点续 Run + 每波末「继续/收枪」正式按钮；收枪×0.5 |
| 死亡螺旋 | 首领波前小回复/免伤；答对也吸血；acc 低停注入 |
| 消极不学新词 | Boss 双驱动 `maxGapDays=4` 强制检验兜底 |
| 新词堆积 / 复习空转 | 注入容量门控 + Q 队列（错词优先）填实复习；千词池注入长期非零 |
| 刷分 | 破纪录按历史 max + `recordBroken` 一次性；收枪收益×0.5；重复早退收益低 |
| 客户端谎报答案 | advance/finish 强制 `typed` 服务端比对，缺失拒收 |
| 并发双结算 | `status='active'` 守卫 + `updateMany count` 校验 + 事务 |
| 中断波丢失 | 待办：每答 keepalive 局部持久化（v1.1） |
| 数值失衡 | 全部走 shared 配置，M3 平衡仿真（v2.4 模型回归断言）+ M7 手调 |
| 合成经济通胀 | 合成走 `updateMany` 乐观锁 + 事务、材料数不足 409；Ⅲ/Ⅳ 受天数解锁与破纪录消耗双向约束，M7 验证 |

## 8. 文件改动总清单（预估）
```
db/schema.prisma 新增 Run/RunItem（含索引/幂等字段）+ 迁移
packages/shared/src/api.ts  新增 Run/RunFinish/DTO
packages/shared/src/game.ts 新增数值表/buff/注入阈值/伤害模型/消耗表
apps/api/src/runs/          (新) controller/module/service + 纯函数模块(注入/boss-trigger/buff-picker)
apps/api/src/runs/balance-sim.ts  (新) M3 平衡仿真核心（v2.4 模型/runTrial/monte/monsterHitsOf/travelBudgetOf）+ balance-sim.spec.ts 目标带断言（10 条）
apps/api/src/materials/        (新) 合成 controller/module/service（POST /materials/synthesize，事务+乐观锁）+ materials.service.spec.ts
apps/api/src/sessions/settlement.ts  重构可复用 + 天数/首领/破纪录奖励
apps/api/src/auth/          strengthen 端点 + DTO
apps/web/src/pages/BattlePage.tsx    Run 状态机
apps/web/src/components/BattleField.tsx  θ/吸血/需答对X题/首领/新词暴击
apps/web/src/pages/CharacterPage.tsx  强化按钮
apps/web/src/pages/StageMapPage.tsx   最高生存天数
apps/web/src/pages/LobbyPage.tsx / ResultPage.tsx 微调
apps/web/src/store/auth.ts  strengthen 后刷新
```

## 9. 明确后置 v1.1
终极战 / 排行榜(PK) / 每答实时持久化 / Edge-TTS / 桌面打包 / E2E 冒烟

---

## 实施进度

| 里程碑 | 状态 | 备注 |
|---|---|---|
| M1 数据与类型 | ⬜ 未开始 | 含 Run 新字段/索引/伤害模型/消耗表 |
| M2 后端核心 | ⬜ 未开始 | 含 boss-trigger |
| M3 纯函数+测试 | ◐ 进行中 | balance-sim.ts + spec 已完成（10 断言，v2.4 模型中位 5–15 达标）；待 inject/review-queue/theta/boss-trigger/buff-picker/合成 |
| M4 前端战斗 | ⬜ 未开始 | 含传说技能状态钩子 |
| M5 前端 Run 流程 | ⬜ 未开始 | 含首领战后传说选择节点 |
| M6 外壳收尾 | ⬜ 未开始 | |
| M7 调参验证 | ⬜ 未开始 | 含首领节奏参数 |